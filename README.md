# I See You (icu)

AI supply chain firewall — scan files for prompt injection, data exfiltration, and obfuscated payloads.

## Quick Start

```bash
pip install -e ".[dev]"
icu scan ./path/to/file
```
